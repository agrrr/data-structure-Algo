#ifndef ex1_h
#define ex1_h

/**
 * Input:
 * 1. Pointer to an array of sorted distinct integers that has been circularly
 *    shifted k positions to the right (k unknown)
 * 2. Array's size
 * Output:
 * Maximal element in the array
 */
int findMax(int* pArr, int size);

#endif /* ex1_h */
